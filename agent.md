# Agent Guidelines

## Requirements

- **Cross-platform support**: This Neovim configuration must work on both **macOS** and **Windows**. All plugin build commands, paths, and shell invocations should be compatible with both operating systems.
